#include <iostream>
using namespace std;
#define MAX_N 1000
#define MAX_M 1000

int n, m;
char s[MAX_N], t[MAX_M];

int dp[MAX_N+1][MAX_M+1];


void solve()
{

}


int main(int argc, char const *argv[])
{
	scanf("%d", &n);
	scanf("%d", &m);
	scanf("%s", s);
	scanf("%s", t);
	
	printf("%s\n", s);
	return 0;
}